
# Arduino_CW_Flasher_keyer
Arduino Sketch to Flash your Callsign in Morse using a switch
Basically the same code as the Arduino_Callsign_Flasher but with an added switch that will repeat the callsign or text inside the Sketch every time the switch is pressed.

